# 候选程序独立复现与结果验收任务

- 状态：in_progress
- 状态说明：Q1 已执行并验收；Q2 程序与条件输出算术已分项验收；Q3 条件优化程序、ODE复排、步长细化和输出谱系已验收，跨机型外推、代理精度与全局最优性仍受限
- 类别：programming
- 所属问题：问题一、问题二、问题三
- 必须使用的 Skill：各问题先由 `problem-modeling-writer` 审查模型，再由 `model-verification-writer` 制定和执行验收；Q1 的源码改动仅由获授权的编程 Subagent 执行

## 总体目标

对 `第4题第一二三问修订源码/` 中候选脚本与 CSV 结果进行独立复现，核对公式实现、输入来源、随机性、输出字段和正文主张。Q2/Q3 的现有验收约束不因本次 Q1 任务而改变。

## Q1 阶段：已批准的修复范围

### 状态

- 执行状态：verified
- 提交日期：2026-08-13
- 验收日期：2026-08-13
- 验收 Skill：`model-verification-writer [问题一]`
- 验收摘要：正式命令在无Shapely环境退出0并直接生成CSV、PNG、JSON；使用 `PYTHONPATH=/tmp/q1-shapely-deps` 完成可选几何交叉检查。独立高精度公式、直接Green积分、20003点守恒与721姿态containment均通过；脚本、CSV、图和Q1具体命题已分项verified。
- 用户已批准：仅为修复 Q1 复现链，可修改 `第4题第一二三问修订源码/q1_geometry.py` 并重新生成 Q1 CSV/图片。
- 禁止将本次授权扩展到 `q2_model.py`、`q3_model.py`、任何正文、`main.tex`、`references.bib`、`cumcmthesis.cls`、`code/` 或其他未批准路径。

### 已确认的模型适用范围

仅对 Q1 采用以下用户批准的理想化：转子三条侧边连接相邻顶点、关于边中线对称，并在边中点与直径 `d` 的内切圆相切，且取劣圆弧。它不是正式附件关于实际转子侧边形状的事实断言；所有容积、接触与灵敏度结论均限于该理想几何，不能外推为制造公差、热变形或密封结论。

### 输入

- `第4题第一二三问修订源码/q1_geometry.py`
- `第4题第一二三问修订源码/q1_results.csv`（已验收为当前脚本直接输出）
- `第4题第一二三问修订源码/figure/q1_corrected_volume_cr.png`（已验收为当前脚本直接输出）
- `.modeling/evidence.yaml` 中 Q1 的 generated/pending/rejected 记录
- `.modeling/issues.md` 的 ISSUE-008 至 ISSUE-011

### 必须修复和核对的内容

1. 修正输出路径：从 Q1 源码目录直接运行时，脚本不得写入不存在的 `writer/figure/`；应在既定 Q1 输出目录生成图片，不依赖人工预建目录。
2. 让同一次正式运行直接写出 `q1_results.csv`，并在代码或 CSV 元数据中记录生成命令、脚本相对路径、关键输入参数和生成时间/版本信息，使 CSV lineage 可追溯。
3. 明确区分周期：单腔**几何容积**最小周期为 540° 曲轴角；同一工作腔的完整**热力状态循环**为 1080° 曲轴角。不得把两者互换。
4. 在已批准圆弧理想化下，计算并分开标注：
   - `d_contact = 150.000 mm`：理论壳体内的条件性实体接触边界；
   - `d_zero = 159.872613647 mm`（按重跑精度报告）：`Vmin(d)=0` 的代数零余隙根，不能标为机械干涉边界；
   - `d=161.7 mm`：只可作为代数模型的非物理解延拓，不可标为物理可行样本。
5. 对 `d` 扫描加入全转角轮廓 containment/最小间隙检查或与其等价、可复核的几何检查；报告检查分辨率、容差与边界判定规则。不得自行加入题目未给定的壁厚、热膨胀或加工裕量阈值。
6. 修正体积术语和下游接口：题设排量为 `3Vmax`；单腔 `Vs` 是在当前单转子 BMEP 定义下用于 `P=BMEP*Vs*n/60` 的 swept-volume；`3Vs` 不得不加说明地代入同一公式。
7. 重生成 Q1 图片，移除/避免“159.8726 mm 即 geometric interference”等错误标注，且图例、CSV、stdout 与脚本计算一致。

### 要求输出

- 修改后的 `第4题第一二三问修订源码/q1_geometry.py`
- 由该脚本在同一次正式运行直接生成的 `第4题第一二三问修订源码/q1_results.csv`
- 由该脚本在同一次正式运行直接生成的 Q1 图（至少 `第4题第一二三问修订源码/figure/q1_corrected_volume_cr.png`；若新增图，须说明用途和 lineage）
- 简短运行日志或机器可读元数据，记录 Python/依赖版本、命令、关键参数、输出文件、扫描分辨率和容差
- 给 `model-verification-writer [问题一]` 的提交说明：列出变更、关键数值、已知局限和对 ISSUE-008--011 的逐项响应

### Q1 验收标准

- 从 Q1 源码目录或项目约定根目录执行下列命令可无人工建目录地完成，退出码为 0：

  ```bash
  python 第4题第一二三问修订源码/q1_geometry.py
  ```

- CSV 与图片由此次运行直接生成；检查文件时间、记录的命令/版本和数值字段，能够闭合“脚本 → CSV/图片”的 lineage。
- 独立复核至少包括：`Vs=3sqrt(3)ReB`、三腔守恒、540° 几何周期、1080° 热力循环、`d_contact=150.000 mm` 条件性接触边界与 `d_zero≈159.872613647 mm` 代数根的语义区分。
- 对 `d=147`、`132.3`、`150.0`、`150.01`、`159.872613647`、`161.7 mm` 报告可复核字段；不得把 150 mm 以上的实体重叠解列为物理可行结果。
- Q1 数值、脚本、CSV、元数据和图片已由 `model-verification-writer [问题一]` 完成本地字段、命令、公式与图表核对；具体状态以 `evidence.yaml` 的命题级记录为准，两份网页submitted报告本身仍保持generated。

### 禁止事项

- 不改变用户批准的圆弧理想化；如发现其内部矛盾，仅报告，不自行改用其他转子曲线。
- 不修改 Q2/Q3 程序、正文、CSV、图片或 `.modeling/` 状态。
- 不把 `3Vs` 直接代入 `P=BMEP*V*n/60`；不把 `d_zero` 命名为机械干涉边界。
- 不执行 git commit、git push 或覆盖性 Git 命令。

### 论文预期位置

- Q1 结果仅在 `problem-1-analysis`、`problem-1` 及其 verification 区经相应 Skill 验收后使用。
- Q2 只可引用经过 Q1 验收的单腔 `Vs` 和明确的 BMEP 口径；不得引用未验收的 `3Vs` 动力学换算。

## Q2 阶段：修复版程序验收结果

- 执行状态：verified
- 验收日期：2026-08-14
- 验收 Skill：`model-verification-writer [问题二]`
- 第二轮已验收：`q2_closed_loop.py` 重复运行确定性、RK4量纲、功与壁热同步积分、1080度循环固定点、单腔 `Vs` 功率口径、三腔360/720度错相转矩、参考压力抵消、1度7x7精确ODE筛查、二次响应代理、三起点受限拟合、候选精确ODE回代、31点重叠扫描、样本内MAPE/RMSE算术、0.5度功/转矩细化及CSV/JSON/PNG哈希链。
- 最终验收：第一轮 `success=false,nfev=3` 的历史命题保持rejected；最新版三起点代理优化均成功，候选均经精确ODE回代，故数值流程verified。但 `friction_scale=1.8` 命中上界，其余6项先验固定，故物理唯一性rejected；42.820137 kW、46.362162 kW、峰压/峰温及效率仍为 generated 跨机型条件输出。
- ISSUE-013最小返修已验收：批量标定、基准、0.5度细化和31点扫描统一使用收敛1080度循环起点fresh charge推得、循环内固定的燃料量；33例定义一致且`eta_b`/BSFC公式误差为0。允许写条件效率响应及“75度为指定网格内条件效率最大点”，不得写目标机实际最优角。ISSUE-014保持open。

## Q3 条件设计返修与验收结果

- 执行状态：verified_numerical_chain_with_conditional_interpretation
- 验收日期：2026-08-14
- 正式候选程序：`q3_conditional_design.py`
- 训练证据：400个七维拉丁超立方ODE点，全部循环收敛；最大状态相对残差`1.998e-5`
- 正式搜索：50000候选；代理仅作筛选，前40候选在5种真实输入扰动情景下完成200次ODE复排
- 推荐点：`d=149.601853 mm`、`overlap=23.261230 deg`、`ign=29.745066 deg BTDC`、`EOI=591.681525 deg`、`lambda=0.968920`
- 1度ODE回代：`52.763364 kW`、`eta_b=0.372483`、`BSFC=222.180812 g/kWh`、`pmax=6.977358 MPa`、`Tmax=2922.083 K`
- 0.5度细化：功率相对差`1.84e-4`，峰压相对差`1.63e-5`
- 限制：代理峰压留出集`R2=0.7824`，不同种子/权重的重叠角推荐约25.35--70.86度；只允许表述为指定候选池、情景与权重下的条件试验起点，不得称全局最优或目标机实测。

## 历史 Q2/Q3 候选（不再作为正式Q3证据）

### 输入

- `q2_model.py` 与 `q2_results.csv`
- `q3_model.py` 与 `q3_results.csv`
- `04-q2.tex`、`05-q3.tex`
- `figure/`

### 禁止事项

- 论文 Skill 不得直接修改上述程序；发现问题应更新本任务书或另行获得编程修改授权。
- 未完成外部来源核验前，不得验收 Q2/Q3 的 BMEP 迁移结论。
- 不得仅以 CSV 与正文数值一致作为模型正确性证明。

### 验收标准

- 记录 Python 环境和依赖；
- 重跑结果与提交 CSV 对比；
- 检查单位、角度制、排量口径、随机种子和边界约束；
- 对关键结果执行独立手算或替代实现交叉校验；
- 确认每幅拟采用图都能追溯到已验收数据；
- 分问题更新 `evidence.yaml` 状态。

### 执行命令

Q2/Q3 待 `model-verification-writer` 审查依赖和输出路径后确定；不得在未确认副作用前盲目运行脚本。
